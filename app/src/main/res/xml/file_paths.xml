<?xml version="1.0" encoding="utf-8"?>
<paths xmlns:android="http://schemas.android.com/apk/res/android">
    <!-- Allows GifVision to expose rendered GIFs through a FileProvider during share flows. -->
    <external-path
        name="external"
        path="." />
    <files-path
        name="internal"
        path="." />
</paths>
